NODE_ENV=production
PORT=3000

# PostgreSQL
POSTGRES_PASSWORD=random_password_here
POSTGRES_USER=misskey
POSTGRES_DB=misskey
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}

# Misskey specific
MISSKEY_URL=https://your-app-name.up.railway.app
MISSKEY_PORT=${PORT}

# SMTP (optional)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=
